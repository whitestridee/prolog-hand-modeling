
getugol(X1, Y1, Z1, X2, Y2, Z2, X3, Y3, Z3):-

abx = X1 - X2,
aby = Y1 - y2,
abz = Z1 - Z2,

bcx = X3 - X2,
bcy = Y3 - Y2,
bcz = Z3 - Z2,

dotprod = abx * bcx + aby * bcy + abz * bcz,

magnitudeab = abx * abx + aby * aby + abz * ab,

magnitudebc = bcx * bcx + bcy * bcy + bcz * bcz,

ugol = (dotprod / sqrt(magnitudeab * magnitudebc)) * 180 / 3.14.
